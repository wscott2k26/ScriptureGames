# The TestFlight-ready app is local-first and works with no backend variables.
# To opt into a deployed FastAPI backend, set BOTH values:
EXPO_PUBLIC_USE_REMOTE_API=false
EXPO_PUBLIC_BACKEND_URL=https://your-api.example.com
